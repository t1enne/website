---
interface Props {
  title: string;
  provider: string;
  dates: string;
}

const { title, provider, dates } = Astro.props;
---

<li class="print:mb-0.5">
  <p class="text-md text-muted">
    <span class="font-semibold text-black">{title}</span> - {provider} - {dates}
  </p>
</li>
