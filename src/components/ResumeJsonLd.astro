---
interface Props {
  name: string;
  jobTitle: string;
  email: string;
  linkedin: string;
  skills: string[];
  languages: Array<{ name: string; level: string }>;
}

const { name, jobTitle, email, linkedin, skills, languages } = Astro.props;

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Person",
  name,
  jobTitle,
  email,
  url: linkedin,
  sameAs: [linkedin],
  knowsAbout: skills,
  knowsLanguage: languages.map(l => ({
    "@type": "Language",
    name: l.name,
    proficiencyLevel: l.level
  }))
};
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
