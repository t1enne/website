---
interface Props {
  name: string;
  level: string;
}

const { name, level } = Astro.props;
---

<li class="py-2 print:py-0.5 grid grid-cols-2 gap-2">
  <span class="font-semibold print:text-xs">{name}</span>
  <span class="text-muted print:text-xs">{level}</span>
</li>
